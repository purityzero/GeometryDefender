## 2. UI 컴포넌트

### 표준 컴포넌트 기준

| 용도 | 표준 컴포넌트 |
|---|---|
| 이미지/스프라이트 | `UnityEngine.UI.Image` |
| 텍스트 | `TMPro.TextMeshProUGUI` (legacy `Text` 사용 금지) |
| 클릭 버튼 | `UnityEngine.UI.Button` |
| 목록/스크롤 | `UnityEngine.UI.ScrollRect` |

### 확장 클래스 관례
- 프로젝트에 표준 컴포넌트를 상속한 자체 확장 클래스(예: 로컬라이제이션 키 자동 바인딩, 리소스 자동 로드 등 부가 기능)가 있을 수 있다. 신규 컴포넌트 추가 전에 기존 프리팹에서 확장 클래스 사용 관례를 먼저 확인하고, 관례가 있으면 표준 대신 확장 클래스를 쓴다. (레거시 형제 오브젝트가 표준 타입이어도 최신 관례를 따른다)
- 확장 클래스는 필드 구조가 베이스와 대부분 동일하므로, 스크립트 참조(guid)만 교체하면 나머지 직렬화 필드는 그대로 재사용 가능하다.

### YAML 직접 편집 원칙
- 프리팹 파일은 Unity Editor 없이 YAML로 직접 편집한다.
- 컴포넌트 추가 순서: ① GameObject의 `m_Component` 목록에 fileID 추가 → ② 해당 fileID로 컴포넌트 블록 추가 (순서가 바뀌면 파싱 실패나 missing 컴포넌트로 이어진다)
- fileID는 파일 내 고유해야 하며, 신규 추가분은 기존 패턴을 따라 구분되는 대역을 사용한다. (예: `9001000000000000001`) 단, 정수형 최댓값 범위를 넘지 않도록 주의한다 — 너무 큰 값은 파싱 자체가 실패하는 경우가 있다.
- GUID는 추측하지 않고 반드시 실제 파일에서 확인한다. (`.cs.meta` 파일 대조 또는 기존 프리팹 grep)

### 신규 블록 작성 순서
1. 동일 컴포넌트가 쓰인 기존 프리팹을 grep으로 탐색
2. 해당 블록의 YAML 포맷을 그대로 복사
3. `m_GameObject`, `m_TargetGraphic` 등 참조 fileID만 대상에 맞게 교체

### 편집 시 흔한 문제
- 버튼의 타겟 그래픽 참조가 비어있거나, 대상 이미지의 레이캐스트 대상 옵션이 꺼져 있으면 클릭이 아예 안 잡힌다 — 클릭 안 되는 버그의 상당수가 이 케이스다.
- 빈 영역(배경 없는 슬롯 등)에 클릭을 받게 하려면 투명하지만 레이캐스트는 받는 이미지를 별도로 추가해 버튼의 타겟 그래픽으로 연결하는 "투명 클릭 영역" 패턴을 사용한다.
- 중첩된 프리팹(예: 공용 위젯)에 내장된 자체 버튼이 있으면, 부모 쪽 클릭 핸들러로 이벤트가 버블링되지 않고 먼저 가로챈다 — 부모가 클릭을 받아야 하는 구조라면 자식의 내장 버튼을 비활성화한다.
- 프리팹 오버라이드가 존재하지 않는 참조 ID를 가리키면 **에러 없이 조용히 무시**되는 경우가 많다(missing 표시도 안 뜸). 텍스트/이벤트가 안 먹히는데 원인을 못 찾겠으면, 참조 ID가 대상 프리팹에 실제로 존재하는지부터 의심한다.
- 편집 후 실시간 컴파일/플레이 테스트를 못 하는 환경이 많다는 걸 전제로 한다 — 작업 완료 시 "미검증" 여부를 명시하고, 실제 실행 확인을 받을 때까지 완료로 단정하지 않는다.

---

## 3. 자주 쓰는 UI 패턴 레시피

### 스크롤 목록 — 항목 수가 적을 때 (수십 개 이하)
- `ScrollRect` + `Content`에 `VerticalLayoutGroup`/`HorizontalLayoutGroup` + `ContentSizeFitter`(PreferredSize) 조합 + 단순 `Instantiate` 반복이면 충분하다.
- 항목 개수가 자주 바뀌는 화면은 매번 전체를 파괴 후 재생성하지 말고, 기존 개수와 새 개수를 비교해 **초과분만 반납/제거, 부족분만 추가, 겹치는 인덱스는 데이터 세팅만 재호출**(증분 갱신)하면 갱신 비용을 크게 낮출 수 있다.
- 아이템을 재사용할 때는 논리적 순서와 하이어라키상의 Sibling Index를 매번 명시적으로 맞출 것(`transform.SetSiblingIndex(i)`). LayoutGroup은 자식의 Sibling Index로 화면 순서를 결정하므로, 재사용 이력에 따라 우연히 순서가 맞다가 특정 조건에서 깨지는 버그가 생길 수 있다.

### 스크롤 목록 — 항목 수가 많을 때 (수백 개 이상, 가상 스크롤)
- 표준 `ScrollRect`는 가상화를 기본 제공하지 않는다. 항목이 많으면 화면에 보이는 개수만큼만 GameObject를 만들고, `ScrollRect.onValueChanged` 콜백에서 스크롤 위치에 따라 이 소수의 아이템들을 재배치 + 데이터만 바꿔치기하는 방식(가상화)을 직접 구현하거나 검증된 라이브러리를 사용한다.
- 직접 구현 시 핵심: 실제 스크롤 영역 크기는 "전체 항목 수 × 항목 높이"로 계산한 더미 크기로 유지하고, 실제로 생성하는 GameObject 수는 뷰포트에 보이는 개수 + 여유분 정도로 고정한다.
- 특정 인덱스로 스크롤 이동(예: 현재 진행 상태로 자동 스크롤)이 필요하면, 행 높이가 균일하다는 전제하에 `verticalNormalizedPosition = 1f - targetIndex / (totalCount - 1)` 같은 비율 계산으로 근사 가능하다. 정확한 픽셀 계산이 필요 없다면 실용적으로 충분하다.
- **주의**: 패널이 비활성 상태이거나 방금 활성화된 직후에는 레이아웃 컴포넌트가 아직 반영되지 않은 상태라 스크롤 위치 계산이 어긋날 수 있다. "활성화 → `Canvas.ForceUpdateCanvases()`로 레이아웃 강제 갱신 → 스크롤 위치 계산" 순서를 지킬 것.

### 오브젝트 풀링
- 매번 생성/파괴하는 대신 재사용한다. 순수 Unity 기준으로는 `UnityEngine.Pool.ObjectPool<T>`(Unity 2021+ 내장) 사용을 우선 검토하고, 프로젝트에 이미 자체 풀링 유틸리티가 있으면 그걸 재사용한다.
- 상위 프레임워크의 자동 생명주기 훅을 안 타는 하위 구성요소라면, 풀 생성 자체를 첫 사용 시점까지 지연 생성(lazy init)하는 것도 방법이다.

### 2-상태 토글형 버튼 (예: On/Off, 활성/비활성)
- 표준 `Button` 하나로 처리하려면: 클릭 콜백 안에서 현재 상태를 보고 분기 처리 + 상태별로 자식 GameObject(라벨/배경) 활성화를 전환하는 방식이 가장 단순하다.
- 상태별 GameObject를 완전히 분리해 각각에 별도 `Button`을 두는 방식(상태 전환 = GameObject 활성화 전환)도 가능하지만, **원래 "비활성 표시 전용"으로만 설계된 상태(회색조 등)에 나중에 실제 클릭 액션을 추가하려면 그 GameObject에 `Button` 컴포넌트 자체가 존재하는지부터 확인**할 것 — 순수 시각 효과용으로만 만들어둔 상태엔 애초에 클릭 컴포넌트가 없어서, 코드에서 리스너를 등록해도 조용히 무시되는 경우가 있다.
- 비활성 사유가 여러 개(예: 조건 미충족/자원 부족/최대치 도달)인 화면은 "비활성 = 한 가지 표시"로 뭉뚱그리지 말고 사유별 오브젝트를 분리해서, 정확한 사유만 켜고 나머지는 코드에서 강제로 꺼줄 것.

### 값 표시 스케일 변환
- 정수 필드가 사실은 퍼센트/배율의 정수 인코딩인 경우(예: 10000=100%, 1,000,000=100%) 데이터 종류마다 스케일이 다를 수 있다. 새 변환 로직을 만들기 전에 같은 프로젝트 내 이미 존재하는 동일 계열 변환 코드의 스케일을 먼저 확인한다.